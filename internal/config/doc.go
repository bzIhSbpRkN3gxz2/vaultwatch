// Package config provides loading and validation of vaultwatch configuration
// from YAML files. It defines the full configuration schema and exposes
// sensible defaults for all optional fields.
package config
