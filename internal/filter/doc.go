// Package filter provides composable filtering of Vault lease slices.
// Filters can be combined by path prefix, lease status, and TTL range
// to narrow down which leases are acted upon by monitors or alerters.
package filter
